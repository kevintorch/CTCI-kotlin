package Big_O.Additional_Problems

/**
 * If a binary search tree is not balanced, how long might it take (worst case) to find an element in it?
 *
 * O(N)
 *
 * where n is the number of nodes in the tree.The max time to find an element is the depth tree.
 * The tree could be a straight list downwards and have depth n.
 */